// We can crete JS file using .js extention
// In old days the software that complie the JS code is Come inside the browser only, but it limit the uses of it then we seperate the engine like V8 engine from browser to create a seperate engines like node.js or deno etc.

console.log("Tejas")        // used to show the content in browser

// node -v    -> For checking version
//  node 01_test.js